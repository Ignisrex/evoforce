package com.silverignis.skills;

/**
 * Marker interface for shape-specific configuration data on a {@link Skill}.
 * Each shape can define its own implementation with fields relevant to that
 * shape's behavior (e.g. {@link ProjectileConfig}).
 */
public interface ShapeConfig {
}
